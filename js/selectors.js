const formSpend = document.querySelector('#formSpends');

const nameSpend = formSpend.querySelector('#nameSpend');
const priceSpend = formSpend.querySelector('#priceSpend');
const typeSpend = formSpend.querySelector('#typeSpend');

const boxCategoryHtml = document.querySelector('#categoriesBox');
const boxSpendHtml = document.querySelector('#allSpends');

const yearFooter = document.querySelector('footer span');

export {formSpend,nameSpend,priceSpend,typeSpend,boxCategoryHtml,boxSpendHtml,yearFooter};